{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/dhihm/malgwi/schema/lesson-v2.schema.json",
  "title": "lesson-v2",
  "description": "Static study-page lesson document pairing one source-language video with one study language. The model authors only pronunciation and translation (and optional glossary meanings); original text and timecodes are bound to the capture via source_digest. Legacy v1 documents (Korean-only field names) are upgraded by consumers before validation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "video",
    "study_language",
    "source_digest",
    "lines"
  ],
  "properties": {
    "schema_version": {
      "const": 2
    },
    "video": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "provider",
        "video_id",
        "source_language"
      ],
      "properties": {
        "provider": {
          "const": "youtube"
        },
        "video_id": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]{11}$"
        },
        "source_language": {
          "type": "string",
          "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]{2,8})*$"
        },
        "title": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "study_language": {
      "type": "string",
      "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]{2,8})*$",
      "description": "The learner's language; pronunciation, translation, glossary meanings, and the runtime UI follow it."
    },
    "display": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "mode"
      ],
      "description": "Omitted means embed: in-page IFrame playback with line sync. sheet renders the lines standalone with timestamp links out to the video.",
      "properties": {
        "mode": {
          "enum": [
            "embed",
            "sheet"
          ]
        }
      }
    },
    "source_digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 hex over the canonical JSON of the captured caption list [{start_ms,end_ms,text}...]."
    },
    "lines": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "start_ms",
          "end_ms",
          "original",
          "pronunciation",
          "translation"
        ],
        "properties": {
          "start_ms": {
            "type": "integer",
            "minimum": 0
          },
          "end_ms": {
            "type": "integer",
            "exclusiveMinimum": 0
          },
          "original": {
            "type": "string",
            "minLength": 1
          },
          "pronunciation": {
            "type": "string",
            "minLength": 1,
            "description": "Source speech rendered in the learner's script."
          },
          "translation": {
            "type": "string",
            "minLength": 1,
            "description": "The line in the learner's language."
          },
          "sentence_end": {
            "type": "boolean",
            "description": "Model-authored sentence boundary: true = a sentence ends with this cue, false = it definitely continues, omitted = display-time heuristics decide. Partial annotation is expected."
          }
        }
      }
    },
    "glossary": {
      "type": "array",
      "description": "Optional model-authored glosses (learner's language) for normalized lowercase words or short phrases; usage examples are derived from the lines at display time.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "word",
          "meaning"
        ],
        "properties": {
          "word": {
            "type": "string",
            "minLength": 1
          },
          "meaning": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
